
<div class="containerStepper">
    <div class="formContainerStepper">
        <form action="#" method="post" class="form_geral">
            <fieldset class="page slidePage" id="parceiro">
                <header>
                    <h1>Personalize</h1>
                    <p>O clube é feito para você, personalize-o conforme as suas preferências</p>
                </header>

                <div class="parceiro">

                        @for($i=1; $i<40; $i++):
                        <div class="opcao">
                            <input id="{{ $i }}" name="tag" type="checkbox" value="{{ $i }}">
                            <label for="{{ $i }}">Parceiro - {{ $i }}</label>
                        </div>
                        @endfor;

                </div>


                <div class="footer_button primeiro">
                    <button id="botao_favorito" class="proxima">Avançar</button>
                </div>
            </fieldset>

            <fieldset class="page slidePage" id="favorito">
                <header>
                    <h1>Selecionamos uma lista de parceiros conforme as suas preferências, deseja favoritar algum deles?</h1>
                    <p>Ao favoritar esses parceiros irão aparecer no topo da sua lista ao entrar no clube, basta clicar no botão nos blocos abaixo</p>
                </header>

                @view site/loja/geral/parceiro.view

                <div class="footer_button">
                    <button class="voltar">Voltar</button>
                    <button class="proxima" id="botao_envia_preferencia">Enviar</button>
                </div>
            </fieldset>

        </form>
    </div>
</div>

