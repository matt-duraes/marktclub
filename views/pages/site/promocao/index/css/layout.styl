@system "Loading"
@system "Pagina"
@template "site"
@system "Galeria"
@resource "site/variavel"

#bloco_promocao
    maxWidth()
    article
        flexCol()
        boxShadow()
        cursor: pointer
        border-radius: 3px
        margin-bottom: 22px
        borderRadiusPadrao()
        overflow: hidden
    article figure
        width: 100%
        height: 250px
        background-size: cover
        background-position: top center
