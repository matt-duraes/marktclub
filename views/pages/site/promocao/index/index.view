@template "site"

<div id="bloco_promocao">
    @foreach($promocoes as $dado):
    <article>
        <figure style="background-image: url({{$dado->imagem}})"></figure>
        <a class="botao_geral_cor" href="{{$dado->link->link}}" target="_self" >ACESSAR PARCEIRO</a>
    </article>
    @endforeach;
</div>

