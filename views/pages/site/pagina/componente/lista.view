<div class="com_lista" {!!strCssMargem($r->margem_topo, $r->margem_direita, $r->margem_baixo, $r->margem_esquerda)!!}>
@php
    componenteLoading($r->lista);
@end

@php echo $r->lista_tipo == 'bola' ? '<ul>' : '<ol>';

@ foreach($r->lista_valor as $li):
    <li>
        {!!strLang($li)!!}
    </li>
@ endforeach;

@php echo $r->lista_tipo == 'bola' ? '</ul>' : '</ol>';
</div>
