.com_pai_relacionado
    maxWidth()
    flexCol()
    > header
        width: 100%
        flexRow()
        align-items: center
        margin-bottom: 10px
    > header h1
        flexGrow()
        titulo()
    > header h1 span
        titulo()
    > header a
        texto()
