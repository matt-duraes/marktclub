@template "site"
<form action="">
    <input type="hidden" id="input_url" value="{{$url}}">
</form>
<div id="bloco_pagina">
    @ foreach( $html as $r ):
        @view 'site/pagina/componente/funcao'
    @ endforeach;
</div>
