
<div class="modal_cupom_site">
    <i class="fechar" id="botao_fechar_cupom" data-font=""></i>
    <div class="header">
        <figure>
            <img src="{{$dado->parceiro->imagem}}" alt="">
        </figure>
        <div class="dado"> 
            <h1>{{$dado->parceiro->nome}}</h1>
            <p>{{$dado->descricao}}</p>
        </div>
    </div>
    @if($dado->tipo === "cupom")
        <div class="cupom">
            <input type="text" readonly="" id="input_cupom_valor" value="{{$dado->cupom}}">
            <button class="bg_cor" id="botao_copiar">COPIAR CUPOM</button>
        </div>
    @else 
        <div class="cupom">
            <!-- <input type="text" readonly="" id="input_cupom_valor" value="{{$dado->texto}}"> -->
            <input type="text" readonly="" id="input_cupom_valor"
            value="Este parceiro aplica os descontos diretamente no site, sem necessidade de cupom, para ver os desconto, clique no botão abaixo:">
        </div>
    @endif
    <div id="acessar_cupom">
        <a href="{{$dado->link}}" target="_blank" >
            <button>IR PARA LOJA</button>
        </a>
    </div>
    
</div>

