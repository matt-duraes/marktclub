<form id="bloco_form_buscar" class="bloco_form_login form_geral" action="/" method="post">
    <div class="conteudo">
        <header>
            <h1>BUSCAR CONTA</h1>
            <p>Preencha os campos abaixo para buscar sua conta</p>
        </header>

        @ if( $tipoSiape ):
        @? formInput(name: 'buscar', label: 'Buscar', placeholder: 'Digite seu SIAPE');
        @ elseif( $tipoMatricula ):
        @? formInput(name: 'buscar', label: 'Buscar', placeholder: 'Digite sua matrícula');
        @ else:
        @? formCpf(name: 'buscar', label: 'Buscar', placeholder: 'Digite seu CPF')
        @ endif;

        <div class="bloco_botao">
            <div class="botao" id="botao_buscar_usuario">Buscar</div>
        </div>
    </div>
</div>
