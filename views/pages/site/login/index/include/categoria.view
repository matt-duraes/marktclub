
@if ($temMais) :
    @if(sessao('CLUBE') == '122f833e84a78931e6fdcfa2e2a58771') :
        <section id="banner_secundario">
            <div class="imagem">
                <figure style="background-image: url(https://clube.marktclub.com.br/images/login/banner_demais.png)">
                    <img src="https://clube.marktclub.com.br/images/login/banner_demais.png">
                </figure>
            </div>
        </section>
    @else:
        <section class="bloco_categoria" id="bloco_categoria">
            <div class="icone">
                @php
                    $primeiro =  true;
                @end
                @foreach ($temMais as $r) :
                    <i class="{{$primeiro ? 'cor_bg hover' : ''}}" data-id="{{ $r['id']}}">
                        <span>{!!$r['icone']!!}</span>
                    </i>
                    @php
                        $primeiro =  false;
                    @end
                @endforeach;

            </div>

            <div class="conteudo">
                @php
                    $primeiro =  true;
                @end
                @foreach ($temMais as $r) :

                    <article class="cartao {{$primeiro ? 'categoria_atual' : '' }}" id="{{$r['id']}}">
                        <i>
                            <span>{!!$r['icone']!!}</span>
                        </i>
                        <div class="item">
                            <header>
                                <h1>
                                    {{$r['titulo']}}
                                </h1>
                                <p>
                                    {!!$r['texto']!!}
                                </p>
                            </header>

                            <div class="parceiro">
                                @foreach ($r['parceiro'] as $parceiro) :
                                    <div class="imagem">
                                        <figure style="background-image: url({{$parceiro->imagem}})"></figure>
                                    </div>
                                @endforeach;
                            </div>

                            <footer>
                                <h2>
                                    {!!$r['desconto']!!}
                                </h2>
                                @if (array_key_exists('observacao', $r)) :
                                    <p>
                                        {{$r['observacao']}}
                                    </p>
                                @endif;
                            </footer>
                        </div>
                    </article>
                @php
                    $primeiro =  false;
                @end
                @endforeach;

            </div>
        </section>
    @endif;
@endif;

<section id="carrousel">
    <div class="controle-embrulho">
        <div class="controle-slide">
            @php
                $primeiro =  true;
            @end
            @foreach ($temMais as $r) :
                <div class="area-icone">
                    <i>
                        <span>{!!$r['icone']!!}</span>
                    </i>
                </div>
                @php
                    $primeiro =  false;
                @end
            @endforeach;
        </div>
    </div>
    <div class="embrulho-slide">
        <div class="area-slide">
            @php
                $primeiro =  true;
            @end
            @foreach ($temMais as $r) :
                <div class="item-slide">
                    <header>
                        <h1>
                            {{$r['titulo']}}
                        </h1>
                        <p>
                            {!!$r['texto']!!}
                        </p>
                    </header>

                    <div class="parceiro">
                        @foreach ($r['parceiro'] as $parceiro) :
                            <div class="imagem">
                                <figure style="background-image: url({{$parceiro->imagem}})"></figure>
                            </div>
                        @endforeach;
                    </div>

                    <footer>
                        <h2>{!!$r['desconto']!!}</h2>
                        @if (array_key_exists('observacao', $r)) :
                            <p>
                                {{$r['observacao']}}
                            </p>
                        @endif;
                    </footer>
                </div>
                @php
                    $primeiro =  false;
                @end
            @endforeach;
        </div>
    </div>
</section>
