


<div class="bloco_pagina_popup" id="bloco_faq_favorito">
    <header class="header_pagina_popup">
        <h1>Título do Popup</h1>
        <i class="fechar botao_fechar_popup">@iconeFechar</i>
    </header>
    <div class="conteudo_pagina_popup">
        <div class="conteudo">
            <figure class="figure_popup" style="background-image: url( {{$popup->imagem}})">
            </figure>
            <p>{{$popup->texto}}</p>
            <p>{{$popup->regulamento}} </p>
            <a href="{{$popup->botao_link}}" target="{{$popup->botao_target}}">Ler Regulamento</a>
        </div>
    </div>
</div>
