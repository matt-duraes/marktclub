<section class="panel">

  <header>
    <h1>{{$tituloPanel}}</h1>
    <p>{{$subtituloPanel}}</p>
  </header>

  <div class="conteudo">
    @ ifforeach($button as $r):
        <a href="{{$r->link}}" class="button">{{$r->texto}}</a>
    @ endifforeach;
  </div>

</section>
