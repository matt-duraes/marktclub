@template "painel"

<div id="bloco_data_policy_proposicao_detalhe">
    <header>
        <h1>{{$r->titulo}}</h1>
    </header>

    <div class="lista">
        @ if( isset($r->link) ):
        <div class="link">
            <a href="{!!$r->link!!}" target="_blank">
                <i>@iconeLink</i>
                <p>ACESSAR LINK</p>
            </a>
        </div>
        @ endif;

        <div class="bloco" data-ajuda="{{$r->nome}}">
            <span>Nome:</span>
            <p>{{$r->nome}}</p>
        </div>

        <div class="bloco" data-ajuda="{{$r->fonte}}">
            <span>Fonte:</span>
            <p>{{$r->fonte}}</p>
        </div>

        <div class="bloco" data-ajuda="{{$r->data}}">
            <span>Data:</span>
            <p>{{$r->data}}</p>
        </div>

        <div class="linha"></div>

        @ if( isset($r->propriedade) ):
        <div class="bloco" data-ajuda="{{$r->propriedade}}">
            <span>Prioridade:</span>
            <p>{{$r->prioridade}}</p>
        </div>
        @ endif;

        <div class="bloco" data-ajuda="{{$r->tipo}}">
            <span>Tipo:</span>
            <p>{{$r->tipo}}</p>
        </div>

        @ if( isset($r->tipo_decisao) ):
        <div class="bloco" data-ajuda="{{$r->tipo_decisao}}">
            <span>Tipo desição:</span>
            <p>{{$r->tipo_decisao}}</p>
        </div>
        @ endif;

        @ if( isset($r->numero) ):
        <div class="bloco">
            <span>Número:</span>
            <p>{{$r->numero}}</p>
        </div>
        @ endif;

        <div class="bloco"  data-ajuda="{{$r->status}}">
            <span>Status:</span>
            <p>{{$r->status}}</p>
        </div>
    </div>

    @ if( isset($r->texto) ):
    <div class="bloco_texto" id="bloco_texto">
        <div class="texto">{!!$r->texto!!}</div>
        <div class="botao_mostrar hide" id="botao_mostrar_mais">MOSTRAR TUDO</div>
        <div class="degrade hide" id="bloco_degrade"></div>
    </div>
    @ endif;

    @ ifisset($r->tramitacao):
    <div class="tramitacao" id="bloco_tramitacao">
        <h2>Tramitações</h2>
        @ foreach( $r->tramitacao as $rT ):
        <article>
            <h1>{{$rT->titulo}}</h1>
            <div class="data">{{$rT->data}}</div>
            @ if( !empty($rT->link) ):
            <a href="{!!$rT->link!!}" target="_blank">LINK</a>
            @ endif;
        </article>
        @ endforeach;
        <div class="botao_mostrar hide" id="botao_mostrar_mais">MOSTRAR TUDO</div>
        <div class="degrade hide" id="bloco_degrade"></div>
    </div>
    @ endif;
</div>
