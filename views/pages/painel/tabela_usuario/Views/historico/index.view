@template "painel"

@foreach($lista as $l)
    <div>
        {{$l['usuario']['nome']}}
        {{$l['empresa']['nome']}}
        {{$l['erro']}}
        {{$l['novo']}}
        {{$l['atualizado']}}
        {{$l['tipo']}}
        {{$l['status']}}
    </div>
@endforeach
