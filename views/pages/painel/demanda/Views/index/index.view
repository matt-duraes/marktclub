@template "painel"

<div class="bloco_kambam_index" id="bloco_demanda_index">
    <input type="hidden" id="input_area" value="{{$area}}">
    <div class="conteudo_geral">
        @ foreach($quadro as $ind => $item):
        <section class="bloco_coluna">
            <header>
                <h1>{{$item['titulo']}} <span>({!!count($item['lista'])!!})</span></h1>
                <div class="flex"></div>
                @ if( $ind == 0 ):
                <button id="botao_add_tarefa" class="botao">Add</button>
                @ endif;
            </header>
            <div class="conteudo">
                @ifforeach($item['lista'] as $r):
                @view painel/demanda/index/tarefa.view
                @elseforeach;
                <div class="tarefa_zero">Sem itens<br> no momento</div>
                @endif;
            </div>
        </section>
        @endforeach;
    </div>
</div>
