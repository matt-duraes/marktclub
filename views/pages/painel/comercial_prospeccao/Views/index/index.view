@template "painel"

<div class="bloco_kambam_index" id="bloco_comercial_prospeccao">
    <div class="conteudo_geral">
        <section class="bloco_coluna">
            <header>
                <h1>Abordagem</h1>
                <div class="flex"></div>
                <a href="{{LINK}}/app/add/comercial-prospeccao" class="botao">Add</a>
            </header>
            <div class="conteudo" id="bloco_abordagem" data-bloco="abordagem">
                @ifforeach($abordagem as $r):
                @view painel/comercial_prospeccao/index/item.view
                @elseforeach;
                <div class="item_zero">Sem itens<br> no momento</div>
                @endif;
            </div>
        </section>

        <section class="bloco_coluna">
            <header>
                <h1>Apresentação</h1>
            </header>
            <div class="conteudo" id="bloco_apresentacao" data-bloco="apresentacao">
                @ifforeach($apresentacao as $r):
                @view painel/comercial_prospeccao/index/item.view
                @elseforeach;
                <div class="item_zero">Sem itens<br> no momento</div>
                @endif;
            </div>
        </section>

        <section class="bloco_coluna">
            <header>
                <h1>Negociação</h1>
            </header>
            <div class="conteudo" id="bloco_negociacao" data-bloco="negociacao">
                @ifforeach($negociacao as $r):
                @view painel/comercial_prospeccao/index/item.view
                @elseforeach;
                <div class="item_zero">Sem itens<br> no momento</div>
                @endif;
            </div>
        </section>

        <section class="bloco_coluna">
            <header>
                <h1>Em Avaliação</h1>
            </header>
            <div class="conteudo" id="bloco_avaliacao" data-bloco="avaliacao">
                @ifforeach($avaliacao as $r):
                @view painel/comercial_prospeccao/index/item.view
                @elseforeach;
                <div class="item_zero">Sem itens<br> no momento</div>
                @endif;
            </div>
        </section>

        <section class="bloco_coluna">
            <header>
                <h1>Minuta Enviada</h1>
            </header>
            <div class="conteudo" id="bloco_minuta" data-bloco="minuta">
                @ifforeach($minuta as $r):
                @view painel/comercial_prospeccao/index/item.view
                @elseforeach;
                <div class="item_zero">Sem itens<br> no momento</div>
                @endif;
            </div>
        </section>

    </div>
</div>
