<div id="botao_swiped_esquerdo"></div>

<div id="header_principal" class="{!!$webview ? 'display_none_webview' : ''!!}">
    <i class="botao botao_menu_mobile" data-test="botao_menu_mobile">@iconeMenuMobile</i>
    <figure>
        <a href="@route(index.index)">
            <img class="tema_branco" src="{{CLUBE_LOGO_PRINCIPAL}}" alt="{{CLUBE_TITULO}}">
            <img class="tema_escuro" src="{{CLUBE_LOGO_SECUNDARIA}}" alt="{{CLUBE_TITULO}}">
        </a>
    </figure>
</div>
<div id="header_principal_fixo" class="{!!$webview ? 'display_none_webview' : ''!!}">
    <i class="botao botao_menu_mobile">@iconeMenuMobile</i>
</div>

@ if(!$melhorIdade) :
<nav id="menu_principal" class="menu_fechado {!!$webview ? 'display_none_webview' : ''!!}">
    <div class="bg"></div>
    <div class="conteudo">
        <figure class="{{CLUBE_LOGO_CLASSE}} bloco_logo_menu">
            <a href="@route(index.index)">
                <img class="tema_branco" src="{{CLUBE_LOGO_PRINCIPAL}}" alt="{{CLUBE_TITULO}}">
                <img class="tema_escuro" src="{{CLUBE_LOGO_SECUNDARIA}}" alt="{{CLUBE_TITULO}}">
            </a>
        </figure>
        <div class="perfil desktop" id="bloco_perfil">
            <figure style="background-image: url({{USUARIO_IMAGEM}})"></figure>
            <div class="nome">
                <p class="dados_usuario_menu">Olá {{USUARIO_NOME}}</p>
            </div>
            <div class="email">
                <p class="dados_usuario_menu">{{USUARIO_EMAIL}}</p>
            </div>
            @ if( MENU_PERFIL ):
            <i class="abrir desktop" id="botao_menu_perfil">@iconeOpcao(15)</i>
            @ endif;
        </div>

        @ if( MENU_PERFIL ):
            <div class="bloco_geral_sub_menu desktop display_none" id="bloco_menu_perfil" data-test="bloco_menu_perfil_desktop">
                <a class="botao" id="botao_tema">
                    <i>@iconeTema</i>
                    Mudar Tema
                </a>
                <div id="bloco_menu_perfil">
                    @view site/geral/menu/perfil_menu.view
                </div>
            </div>
        @ endif;
        <div class="menu">
            <!-- MENU PRINCIPAL-->
            @view site/geral/menu/index.view
            <!-- PERFIL MENU QUANDO FOR MOBILE-->
            <div class="mobile" id="bloco_perfil_mobile">
                <div class="categoria">PERFIL</div>
                <div id="bloco_menu_perfil" data-test="bloco_menu_perfil_mobile">
                    <a class="botao" id="botao_tema_mobile" data-test="botao_menu_perfil_tema">
                        <i>@iconeTema</i>
                        Mudar Tema
                    </a>
                    @view site/geral/menu/perfil_menu.view
                </div>
            </div>
            <div class="linha"></div>
            <!-- SUBMENUS-->
            @view site/geral/menu/submenus.view
            <div class="resto"></div>
        </div>

    </div>
</nav>

<main id="conteudo_principal" class="{!!$webview ? 'conteudo_webview' : ''!!}">
@endif
